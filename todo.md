Please implement these analyses and visualizations. Separate them into self-contained scripts or notebooks as needed.

- [x] The group dynamics notebook does not allow you to aggregate data. It should do so similar to the occupancy heatmap script. It also seems to take a long time to load.
- [x] In fact, all of the notebooks should have the trial aggregation and alignment similar to the occupancy heatmap script. Maybe it can be functionized, but this is also why it's so important to verify config/rotation is correct.
- [x] The site discovery events notebook does not properly display an output.
- [x] Reward site proximity script also fails to display output properly.

- [x] Double check that aggregation orientation is correct. (the data almost looks like a few samples are not rotated/reflected correctly..?)
- [x] Approach to simplify analysis and get some clear, simple metrics: Create radii around each reward site, track sheep count near each site throughout trial to collapse the dimensionality down to 12 or so.
- [x] Maybe show the progression of probability of reward site presence over time?
- [x] path length: separate analysis or tool/script to end timing once sheep find final reward site.
  - measure path length to this event.
  - Should serve as a good metric. Also will help having the 'trial end time metric'
- [x] spatial information metric across time
- [x] leader/follower dynamics: identify what a leader means; and identify who leads across time, if this is consistent or not, etc.
- [x] Self avoiding random walk? Learning of visited locations; reduction of return trips?
- [x] What happens when sites are found or not? How does this influence search?
- [x] Add temporal precision- maybe some visualization of progression over time
- [x] Grouping across time? How consistent is flocking throughout the trials?
- [ ] acquire control reward site configurations for plotting & etc. (I might need to address this outside of the computer)
- [ ] Review stats generated by leader_follower.py: normalised leadership entropy and dominant leader fraction per trial/assay — do groups develop a consistent leader over learning?
- [ ] Review stats generated by site_discovery_effects.py: speed & spread change (%) before vs. after each site discovery event — do sheep slow down / cluster after finding food?

## Rigour pass (added 2026-05-21)

- [ ] More control comparisons. Pull CTRL_FAR / CTRL_BARN trials in everywhere experimental configs (A/B/C/D) are summarised — occupancy, path length, flocking, leadership, discovery effects — so each "learning" claim has a no-learning baseline.
- [ ] Null hypothesis testing / falsifiability. For every reported effect, state the null and the test that would falsify it. Build shuffled / permuted baselines (e.g. shuffle sheep IDs, shuffle assay labels within group, time-reverse trials) and report observed metric vs. null distribution rather than just p-values from KW/MWU.
- [ ] Random-walk null model. Simulate correlated random walks matched to per-sheep speed / turn-angle distributions; compare real trajectories' coverage, revisit rate, straightness, and sites-found-by-time against this null. Tests whether observed search is better than chance.
- [ ] Expanded leadership analyses.
  - Continuous leadership score per sheep at each timestep (projection onto centroid velocity), not just argmax — report distribution, autocorrelation, and persistence time.
  - Pairwise directional influence (e.g. time-lagged velocity correlation / Granger-style or transfer-entropy) to identify who-follows-whom beyond frontal position.
  - Stability of leadership rank across trials within a group (Spearman ρ between assays) — does the same individual lead repeatedly?
  - Leadership vs. pioneer-visitor identity: are frontal leaders also the first to discover sites?
- [x] Binomial null for leadership. Under "everyone leads equally," each sheep leads a given frame with p = 1/n; total leadership counts per sheep should be Binomial(T, 1/n). Compare observed counts to this Binomial (chi-square / exact) and to a multinomial shuffle of leader labels across time, both per trial and pooled by assay. Frames a clear falsifiable claim for "consistent leader." *Implemented as new cells at the bottom of `scripts/leader_follower.py`: frame-level chi², run-level chi² / per-sheep binomial, and a block-permutation empirical p-value that preserves the observed run-length distribution.*
